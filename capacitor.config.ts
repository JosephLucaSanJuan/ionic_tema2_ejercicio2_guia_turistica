import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_tema2_ejercicio2_guia_turistica',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
